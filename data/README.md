## Data Folder

This folder contains data files used by the tools:

- `evidence.json`
- `hypothesis.json`
- `scenarios.json`

If the folder is empty, the tool will automatically create the required files and write updates directly to them.